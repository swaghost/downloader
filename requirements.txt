# Instagram Downloader - Minimal Dependencies

# Core Instagram functionality
instaloader>=4.15.3

# GUI framework
PyQt5>=5.15.0

# Image processing
Pillow>=10.0.0

# HTTP requests (instaloader dependency, explicit for clarity)
requests>=2.28.0

# Browser cookie extraction (optional, for direct browser cookie access)
browser-cookie3>=0.19.0

# Video thumbnail extraction (optional, for video preview in carousel)
opencv-python>=4.8.0

# Numerical operations for video analysis
numpy>=1.24.0

# Shot detection for video analysis (optional, for Shot Breakdown feature)
scenedetect[opencv]>=0.6.0

# Beat-Composer dependencies (optional, for music video synchronization)
Cython>=0.29.0   # Required for madmom compilation
librosa>=0.10.0  # Audio analysis and beat detection
madmom>=0.16.1   # Advanced beat/downbeat detection
moviepy>=1.0.3   # Video composition and editing
ffmpeg-python>=0.2.0  # Video rendering and encoding
soundfile>=0.12.0  # Audio file I/O (librosa dependency)
audioread>=3.0.0  # Audio format support (librosa dependency)
mutagen>=1.45.0  # Audio metadata extraction (ID3 tags, etc.)

# VLC-based video player (alternative to Qt multimedia, includes codecs)
python-vlc>=3.0.0
